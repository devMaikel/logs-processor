Utilizar o comando "docker-compose up -d --build" para rodar os containers da aplicação
Dentro do container rodar o comando "php artisan process:Logs" para processar os dados e salvar no banco da forma mais performática
Dentro do container rodar o comando "php artisan process:LogsEloquent" para processar os dados e salvar no banco usando a orm do laravel Eloquent
